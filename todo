add ability to change sort
actually add notifications
make date pickers less buggy
add feedback for login/signup
figure out why the current term keeps reverting (maybe add it to userdata)
figure out error where user keeps having to delete cookies to get page to load